<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">

  <head>
    <title> Funfick</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css' %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application'%>
  </head>
  <header >
    <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
    <a class="navbar-brand" style="color:white" > Funfic</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav mr-auto">

        <%if user_signed_in? %>
          <div class="dropdown">
            <button class="dropbtn"><%= current_user.username %></button>
            <div class="dropdown-content">
              <%=button_to "Profile", user_path, :method => 'get'%>
              <%=button_to "My post", user_posts_index_path, :method => 'get' %>
              <%=button_to "Log out", destroy_user_session_path, method: :delete%>
            </div>
          </div>
          <li class="nav-item active" >
            <button class="btn btn-outline-danger my-2 my-sm-0"  id="btn_new_post">New post</button>
          </li>
        <%else %>
          <li class="nav-item active">

            <button class="btn btn-outline-primary my-5 my-sm-0"  id="btn_sign_up" >Sign up</button>
          </li>
          <li class="nav-item active">
            <button class="btn btn-outline-primary my-2 my-sm-0" type="button" id="btn_sign_in" >Sign in</button>
          </li>
        <%end %>
        <li class="nav-item active">
          <button class="btn btn-outline-primary my-2 my-sm-0" type="button" id="funfics"  >Funfics</button>
        </li>

      </ul>

      <%= render partial: 'search/form' %>

    </div>
  </nav>
  </header>
  <body>
  <div class="container">
    <%= yield %>
  </div>

  </body>



</html>