<% if user_signed_in? %>
  <h2>Add a comment:</h2>
  <div id="comments">
<%= form_with(model: [ @post, @post.comments.build ], local: true) do |form| %>
  <p>
    <%#= form.label :message %>
    <%= form.text_area :message %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
</div>
<% end %>